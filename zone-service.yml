server:
  port: 8081

spring:
  application:
    name: zone-service
  datasource:
    url: jdbc:postgresql://localhost:5432/ecosync_zone_db
    username: postgres
    password: Sachintha@2003
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

iot:
  provider:
    url: http://localhost:9090
    username: "sachintha_p"  
    password: "12345678" 

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka/
  instance:
    prefer-ip-address: true

